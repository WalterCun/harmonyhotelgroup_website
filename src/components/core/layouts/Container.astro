---
import { cn } from "~/utils";

interface Props {
  className?: string;
}


const { className } = Astro.props;
---

<div class={cn("mx-auto max-w-[92%] sm:max-w-[90%] xl:max-w-[90%] 2xl:max-w-[90%] 3xl:max-w-[90%]", className)}>
  <slot />
</div>