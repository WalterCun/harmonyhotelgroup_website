---
import Layout from "core/layouts/Layout.astro";
import { lang_en } from "lib/constants";
interface Props {
	error: unknown;
}

const { error } = Astro.props;
---
<Layout lang={lang_en} title="Error 500">
    <div>{error instanceof Error ? error.message : "Error desconocido"}</div>
</Layout>